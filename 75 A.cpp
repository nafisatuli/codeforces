#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,m,i=0,j=0,nm,x,y;
    string s,s1;
    cin >> n >> m;
    nm=n+m;
    while(n!=0)
    {
        x=n%10;
        if(n%10!=0)
            s[i++]=x;
        n/=10;
    }
    while(m!=0)
    {
        y=m%10;
        if(m%10!=0)
            s1[j++]=y;
        m/=10;
    }
    for(int i=s.length()-1;i>=0;i--)
    {

    }
}
